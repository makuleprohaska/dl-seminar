torch
torchvision
timm>=1.0.0
pytorch-lightning
huggingface_hub
webdataset

# Benchmarking (final-performance eval). Install on the VM when running the
# Kaiko eva benchmark; not needed for training. See benchmark/run_eva.py.
#   pip install kaiko-eva
# The optional online probe (distill/eval/online_eval.py) is torch-only.
